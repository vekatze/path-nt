import {
  core.list {filter, for-each},
  core.text {intercalate, split},
  noa.gen {Gen, gen},
  noa.gen.custom.text {Config, text-gen},
  noa.gen.list {list-gen},
  this.path {separator},
}

constant path-components: gen(list(text)) {
  list-gen(
    text-gen(
      Config {
        min-length := 0,
        max-length := none,
        rune-generator := none,
        rune-acceptor := {
          (r) => {
            and(ne-rune(r, `/`), ne-rune(r, `\0`))
          }
        },
      },
    ),
  )
}

constant paths: gen(text) {
  let Gen {generate, shrink} = path-components;
  Gen {
    generate := {
      (size) => {
        let xs = generate(size);
        intercalate(xs, *separator)
      }
    },
    shrink := {
      (path) => {
        pin path = path;
        let components = split(path, separator);
        let components-list = shrink(components);
        let cands =
          for-each(components-list, (components) => {
            intercalate(components, *separator)
          });
        filter(cands, (cand) => {
          ne-text(path, cand)
        })
      }
    },
  }
}
